{
  "name": "kodigocode",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run scripts/build.ts",
    "test": "bun run test",
    "lint": "bun run lint"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "ink": "^4.0.0",
    "blessed": "^0.1.81",
    "tree-sitter-cli": "^0.21.0",
    "ripgrep": "^13.0.0",
    "simple-git": "^3.21.0"
  }
}
