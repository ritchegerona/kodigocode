import React from 'react';
import { render, Box, Text } from 'ink';

const Dashboard = () => (
  <Box flexDirection="column">
    <Text>KodigoCode TUI Dashboard (prototype)</Text>
    <Text>— placeholder UI —</Text>
  </Box>
);

render(<Dashboard />);
